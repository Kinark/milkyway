import React, {Component} from 'react';
import './styles.scss';

export default class Orcamento extends Component {
   render() {
      return (
         <div>

         </div>
      );
   }
}
